name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

permissions: {}

jobs:
  rust-tests:
    name: Rust Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: src/server
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Remove local cargo config (uses mold linker not available in CI)
        run: rm -f .cargo/config.toml

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          toolchain: 1.88.0
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src/server/target
            src/owpctl/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src/server/Cargo.lock', 'src/owpctl/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy
        run: cargo clippy --locked -- -D warnings

      - name: Run tests
        run: cargo test --locked --verbose

      - name: Check owpctl formatting
        working-directory: src/owpctl
        run: cargo fmt --check

      - name: Run owpctl clippy
        working-directory: src/owpctl
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Run owpctl tests
        working-directory: src/owpctl
        run: cargo test --locked --verbose

  dotnet-tests:
    name: .NET Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: src/plugins/jellyfin
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: '9.0.317'

      - name: Restore dependencies
        run: dotnet restore OpenWatchParty.Tests/OpenWatchParty.Tests.csproj --locked-mode

      - name: Verify lock drift is rejected
        run: ../../../infra/scripts/tests/locked-restore.test.sh

      - name: Build
        run: dotnet build OpenWatchParty.Tests/OpenWatchParty.Tests.csproj --no-restore

      - name: Run tests
        run: dotnet test OpenWatchParty.Tests/OpenWatchParty.Tests.csproj --no-build --no-restore --verbosity normal

  js-lint:
    name: JavaScript Lint & Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '20.20.2'
          cache: npm
          cache-dependency-path: src/clients/jellyfin-web/package-lock.json

      - name: Install JS dependencies
        working-directory: src/clients/jellyfin-web
        run: npm ci

      - name: Lint JS recursively
        working-directory: src/clients/jellyfin-web
        run: npm run lint

      - name: Check JS syntax recursively
        working-directory: src/clients/jellyfin-web
        run: npm run check:syntax

      - name: Run JS tests
        working-directory: src/clients/jellyfin-web
        run: npm test

  release-script-tests:
    name: Release Script Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: '9.0.317'

      - name: Check release scripts
        run: shellcheck .githooks/pre-commit infra/scripts/*.sh infra/scripts/tests/*.sh

      - name: Lint GitHub Actions workflows
        env:
          ACTIONLINT_VERSION: 1.7.7
          ACTIONLINT_SHA256: 023070a287cd8cccd71515fedc843f1985bf96c436b7effaecce67290e7e0757
        run: |
          archive="actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          release_url="https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}"
          curl --fail --location --silent --show-error \
            --output "$RUNNER_TEMP/$archive" \
            "$release_url/$archive"
          printf '%s  %s\n' "$ACTIONLINT_SHA256" "$RUNNER_TEMP/$archive" | sha256sum --check
          tar -xzf "$RUNNER_TEMP/$archive" -C "$RUNNER_TEMP" actionlint
          "$RUNNER_TEMP/actionlint"

      - name: Test pre-commit hook
        run: infra/scripts/tests/pre-commit.test.sh

      - name: Verify least-privilege workflow permissions
        run: infra/scripts/verify-workflow-permissions.py

      - name: Verify immutable dependency pins
        run: infra/scripts/verify-pins.sh

      - name: Test immutable dependency verifier
        run: infra/scripts/tests/verify-pins.test.sh

      - name: Test repository secret scanning
        run: infra/scripts/tests/secret-scan.test.sh

      - name: Test checksum and SBOM validation
        run: infra/scripts/tests/supply-chain.test.sh

      - name: Test hostile release metadata
        run: infra/scripts/tests/update-plugin-manifest.test.sh

      - name: Verify version consistency
        run: infra/scripts/tests/verify-versions.test.sh

      - name: Verify plugin metadata generation
        run: infra/scripts/tests/write-plugin-meta.test.sh

      - name: Test plugin packaging validation
        run: infra/scripts/tests/package-plugin.test.sh

      - name: Verify operational documentation commands
        run: infra/scripts/tests/docs-operations.test.sh

      - name: Build and validate plugin package end to end
        run: infra/scripts/package-plugin.sh

  build-server:
    name: Build Server
    runs-on: ubuntu-latest
    needs: [rust-tests]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Build Docker image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ./src/server
          file: ./infra/docker/server.Dockerfile
          push: false
          load: true
          tags: openwatchparty/session-server:test
          build-args: |
            BUILD_MODE=release
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke-test release image
        run: |
          docker run -d --name owp-ci -p 39000:3000 \
            -e JWT_SECRET='B0vLhmX5ZY1mQ4NfIYBcr8VWxOTQ02cbeQ9x7B3K4ow=' \
            -e ALLOW_INSECURE_NO_AUTH=false \
            openwatchparty/session-server:test
          trap 'docker rm -f owp-ci >/dev/null 2>&1 || true' EXIT
          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:39000/health >/dev/null; then break; fi
            if [[ $attempt -eq 30 ]]; then docker logs owp-ci; exit 1; fi
            sleep 1
          done
          [[ $(docker exec owp-ci id -u) == 1000 ]]
          docker stop --time 10 owp-ci
          docker logs owp-ci 2>&1 | grep -q 'Server shutdown complete'
