# Scripts

Helper scripts for dev workflows, testing, and tooling.

- `serve-demo.sh` lance un serveur statique local pour `clients/web/demo.html`
- `make demo` est l'entrée recommandée (centralisation via Makefile)
